# ── Collaborative Perception Platform — Python Backend ──────
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0

# ASGI server (run with: uvicorn api.server:app --reload)
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.7.0

# YOLO object detection (existing detector dependency)
ultralytics>=8.2.0

# Computer vision (existing detector dependency)
opencv-python>=4.9.0

# HTTP client (used by future AI integration services)
httpx>=0.27.0
